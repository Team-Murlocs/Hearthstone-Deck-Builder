import { Component } from "@angular/core"

@Component({
    moduleId: module.id,
    selector: "app",
    templateUrl: "app.component.html",
    styles: [`
        body {
        padding-top: 70px;
    }
    `]
})
export class AppComponent { }